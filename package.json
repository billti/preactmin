{
  "name": "preactmin",
  "version": "1.0.0",
  "description": "Minimal preact starter site",
  "scripts": {
    "start": "esbuild src/app.tsx --outdir=public/lib --bundle --sourcemap --servedir=public",
    "tsc": "tsc -p ./src --outDir ./bin",
    "servejs": "esbuild bin/app.js --outdir=public/lib --bundle --sourcemap --servedir=public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.8",
    "preact": "^10.19.2",
    "typescript": "^5.3.2"
  }
}
